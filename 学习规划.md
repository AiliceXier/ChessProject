# Unity 国际象棋项目学习规划

## 项目概述

这是一个基于 Unity 的多人在线国际象棋游戏，使用了 Unity Gaming Services (UGS) 云服务实现联网对战功能。

### 技术栈
- **引擎**: Unity (使用 Input System 新输入系统)
- **云服务**: Unity Gaming Services (Cloud Code, Lobby, Cloud Save, Leaderboards, Authentication)
- **网络通信**: WebSocket 实时消息推送
- **棋类逻辑**: Gera.Chess NuGet 包 (第三方国际象棋规则库)
- **UI**: TextMesh Pro
- **序列化**: Newtonsoft.Json

### 项目结构
```
d:\unity\chess
├── Chess/                          # Unity 客户端项目
│   ├── Assets/
│   │   ├── Piece.cs                # 棋子组件
│   │   ├── Player.cs               # 玩家控制器 (核心游戏逻辑)
│   │   ├── Chess.inputactions      # 输入系统配置
│   │   ├── Resources/              # 游戏资源
│   │   │   ├── Board/              # 棋盘模型、材质、预制体
│   │   │   ├── Pawn/               # 兵棋子资源
│   │   │   ├── Knight/             # 马棋子资源
│   │   │   ├── Bishop/             # 象棋子资源
│   │   │   ├── Rook/               # 车棋子资源
│   │   │   ├── Queen/              # 后棋子资源
│   │   │   └── King/               # 王棋子资源
│   │   └── Scenes/
│   │       └── ChessDemo.unity     # 主场景
│   └── ...
└── ChessCloudCode/                 # 云代码服务端项目
    ├── Chess.cs                    # 云服务核心逻辑
    └── ModuleConfig.cs             # 依赖注入配置
```

---

## 第一阶段：基础认知 (建议 1-2 天)

### 学习目标
理解项目整体架构，熟悉各个文件的作用和相互关系。

### 学习内容

#### 1.1 项目结构探索
**文件**: 整个项目目录

**学习要点**:
- 理解 `Chess/` (客户端) 和 `ChessCloudCode/` (服务端) 的分工
- 熟悉 Unity 项目的标准目录结构
- 了解 Resources 文件夹的组织方式

**实践任务**:
- [ ] 在 Unity 中打开项目，查看场景层次结构
- [ ] 查看 ChessDemo.unity 场景中的 GameObject 结构
- [ ] 识别场景中的关键对象：Board、Camera、UI 等

#### 1.2 资源文件分析
**路径**: `Chess/Assets/Resources/`

**学习要点**:
- 每个棋子文件夹包含：Materials (材质)、Mesh (模型)、Prefabs (预制体)、Texture (贴图)
- 理解 Light/Dark 命名规则对应白方/黑方
- 了解 FEN 记谱法 (Forsyth-Edwards Notation)

**关键文件**:
- `Board/Prefabs/Board.prefab` - 棋盘预制体
- `Pawn/Prefabs/PawnLight.prefab` / `PawnDark.prefab` - 兵棋子预制体
- 其他棋子类似...

---

## 第二阶段：客户端核心代码 (建议 3-5 天)

### 2.1 Piece.cs - 棋子组件
**文件**: `Chess/Assets/Piece.cs`

**代码内容**:
```csharp
using UnityEngine;

public class Piece : MonoBehaviour
{
    public Vector3 InitialPos;

    private void Start()
    {
        InitialPos = gameObject.transform.position;
    }
}
```

**学习要点**:
- 这是一个简单的 MonoBehaviour 组件
- 只记录棋子的初始位置
- 理解为什么需要记录初始位置 (可能用于重置或动画)

**思考问题**:
- 为什么 Piece 类如此简单？大部分逻辑在哪里处理？
- 棋子是如何被创建和管理的？

### 2.2 Player.cs - 玩家控制器 (重点)
**文件**: `Chess/Assets/Player.cs` (383 行)

这是客户端最核心的文件，建议分模块学习：

#### 模块 A: 初始化与依赖 (第 1-80 行)
**学习范围**: 类的字段声明到 `InitializePlayer()` 方法

**关键内容**:
- 引用的命名空间分析
  - `Unity.Services.*` - Unity 云服务 SDK
  - `Newtonsoft.Json` - JSON 序列化
  - `TMPro` - 文本渲染
  - `UnityEngine.InputSystem` - 新输入系统

- 重要字段理解:
  ```csharp
  private const string StartingBoard = "rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1";
  ```
  这是国际象棋的标准起始 FEN 字符串

- `Start()` 方法流程:
  1. 初始化 Unity Services
  2. 匿名登录
  3. 订阅玩家消息
  4. 同步棋盘
  5. 初始化玩家信息

**实践任务**:
- [ ] 在 Unity 文档中查找 `UnityServices.InitializeAsync()` 的用法
- [ ] 理解匿名登录的流程
- [ ] 了解 ELO 等级分系统

#### 模块 B: 游戏大厅与匹配 (第 82-140 行)
**学习范围**: `CreateGame()` 到 `JoinLobbyByCode()`

**关键方法**:
- `CreateGame()` - 创建游戏房间
- `JoinLobbyByCode()` - 通过房间码加入游戏
- `SetPov()` - 设置玩家视角 (白方/黑方)

**学习要点**:
- Cloud Code 的调用方式:
  ```csharp
  await CloudCodeService.Instance.CallModuleEndpointAsync<HostGameResponse>(
      "ChessCloudCode", "HostGame");
  ```
- 正则表达式清理输入: `Regex.Replace(lobbyInputCodeText.text, @"\s", "")`

**实践任务**:
- [ ] 理解 `HostGameResponse` 和 `JoinGameResponse` 的数据结构
- [ ] 跟踪创建游戏到开始游戏的完整流程

#### 模块 C: 棋盘渲染与同步 (第 142-200 行)
**学习范围**: `SyncBoard()` 方法

**核心逻辑**:
```csharp
private void SyncBoard(string fen)
{
    var boardState = FenToDict(fen);
    // 1. 清除现有棋子
    // 2. 根据 FEN 生成新棋子
    // 3. 动态加载预制体
    // 4. 设置位置和旋转
}
```

**学习要点**:
- FEN 字符串解析为字典 (`FenToDict`)
- 资源动态加载: `Resources.Load($"{pieceType}/Prefabs/{prefabName}")`
- 棋子朝向控制: `Quaternion.Euler(0, char.IsLower(piece.Value)? 180 : 0, 0)`

**关键算法 - FEN 解析**:
```csharp
private static Dictionary<Tuple<int, int>, char> FenToDict(string fen)
{
    // 解析 FEN 字符串为坐标字典
    // 小写字母 = 黑方, 大写字母 = 白方
    // p=兵, n=马, b=象, r=车, q=后, k=王
}
```

**实践任务**:
- [ ] 手动解析一个 FEN 字符串
- [ ] 理解坐标系统: (0,0)=a1, (7,7)=h8

#### 模块 D: 移动逻辑 (第 202-260 行)
**学习范围**: `MakeMove()` 到 `OnBoardUpdate()`

**关键方法**:
- `MakeMove()` - 调用服务端验证并执行移动
- `OnBoardUpdate()` - 处理棋盘更新消息
- `OnGameStart()` - 游戏开始时的初始化

**学习要点**:
- 位置到 FEN 坐标的转换: `PosToFen()`
- 游戏结束处理
- 消息推送机制

#### 模块 E: 输入处理与交互 (第 262-340 行)
**学习范围**: `SubscribeToPlayerMessages()` 到 `PlayerInteract()`

**核心逻辑**:
```csharp
public void PlayerInteract(InputAction.CallbackContext context)
{
    // 1. 射线检测获取点击对象
    // 2. 判断点击的是棋盘还是棋子
    // 3. 选中棋子或执行移动
}
```

**学习要点**:
- 新输入系统的回调方式
- 射线检测: `Physics.Raycast(rayOrigin, out var hitInfo)`
- 棋子选中高亮: `ChangeMaterialColor()`
- 颜色切换逻辑

**实践任务**:
- [ ] 查看 `Chess.inputactions` 文件，理解输入配置
- [ ] 在场景中测试射线检测

#### 模块 F: 数据结构定义 (第 342-383 行)
**学习范围**: 内部类定义

```csharp
public class HostGameResponse { public string LobbyCode { get; set; } }
public class JoinGameResponse { ... }
public class BoardUpdateResponse { ... }
```

---

## 第三阶段：服务端云代码 (建议 2-3 天)

### 3.1 ModuleConfig.cs - 依赖注入配置
**文件**: `ChessCloudCode/ModuleConfig.cs`

**代码内容**:
```csharp
using Microsoft.Extensions.DependencyInjection;
using Unity.Services.CloudCode.Apis;
using Unity.Services.CloudCode.Core;

namespace ChessCloudCode;

public class ModuleConfig : ICloudCodeSetup
{
    public void Setup(ICloudCodeConfig config)
    {
        config.Dependencies.AddSingleton(GameApiClient.Create());
        config.Dependencies.AddSingleton<IPushClient, PushClient>(_ => PushClient.Create());
        config.Dependencies.AddSingleton(new Random());
    }
}
```

**学习要点**:
- 依赖注入 (DI) 模式
- 单例模式 (Singleton)
- `GameApiClient` - 访问 Unity 游戏服务的客户端
- `IPushClient` - 消息推送客户端

### 3.2 Chess.cs - 云服务核心逻辑 (重点)
**文件**: `ChessCloudCode/Chess.cs` (304 行)

#### 模块 A: 类结构与构造函数 (第 1-30 行)
**学习要点**:
- 常量定义: LeaderboardId, KValue (ELO 计算K值), StartingElo
- 构造函数注入依赖
- 使用的第三方库: `Gera.Chess`

#### 模块 B: 创建游戏 (第 32-50 行)
**方法**: `HostGame()`

**流程**:
1. 创建 Lobby (游戏大厅)
2. 初始化棋盘状态 (FEN)
3. 保存到 Cloud Save
4. 返回房间码

**学习要点**:
- `IGameApiClient.Lobby.CreateLobbyAsync()`
- `IGameApiClient.CloudSaveData.SetCustomItemBatchAsync()`
- Cloud Save 数据存储

#### 模块 C: 加入游戏 (第 52-110 行)
**方法**: `JoinGame()`, `StartGame()`, `Rejoin()`

**流程分析**:
```
JoinGame
├── 尝试加入 Lobby
├── 成功 → StartGame (新游戏)
└── 失败 → Rejoin (重新连接)
```

**StartGame 逻辑**:
1. 获取对手 ID
2. 随机分配黑白方
3. 初始化棋盘
4. 保存游戏状态
5. 向对手发送消息
6. 返回游戏信息

**学习要点**:
- 异常处理与重连机制
- 消息推送: `_pushClient.SendPlayerMessageAsync()`
- JSON 序列化

#### 模块 D: 移动验证与执行 (第 112-200 行)
**方法**: `MakeMove()`

**核心逻辑**:
```csharp
public async Task<BoardUpdateResponse> MakeMove(...)
{
    // 1. 获取游戏状态
    // 2. 验证玩家身份和回合
    // 3. 验证移动合法性 (使用 Gera.Chess)
    // 4. 执行移动
    // 5. 保存新状态
    // 6. 检查游戏结束
    // 7. 更新 ELO 分数
    // 8. 通知对手
}
```

**关键验证**:
```csharp
// 检查是否轮到该玩家
if (chessBoard.Turn != playerColour)
    throw new Exception("Invalid move, not active player");

// 检查移动是否合法
if (!chessBoard.IsValidMove(new Move(fromPosition, toPosition)))
    throw new Exception($"Invalid move from {fromPosition} to {toPosition}");
```

**学习要点**:
- `Gera.Chess` 库的基本用法
- `ChessBoard` 类的关键方法:
  - `LoadFromFen()` - 从 FEN 加载
  - `ToFen()` - 导出为 FEN
  - `IsValidMove()` - 验证移动
  - `Move()` - 执行移动
  - `IsEndGame` - 检查游戏结束
  - `EndGame` - 获取结束信息

#### 模块 E: ELO 等级分计算 (第 202-240 行)
**方法**: `UpdateElos()`

**算法**:
```csharp
// ELO 计算公式
var expectedScore = 1 / (1 + Math.Pow(10, (opponentElo - playerElo) / 400));
var eloChange = KValue * (playerScore - expectedScore);
var playerNewElo = playerElo + eloChange;
var opponentNewElo = opponentElo - eloChange;
```

**学习要点**:
- ELO 评分系统原理
- 异步任务并行执行: `Task.WhenAll()`

#### 模块 F: 认输功能 (第 242-304 行)
**方法**: `Resign()`

**流程**:
1. 获取游戏状态
2. 确定玩家颜色
3. 执行认输
4. 更新 ELO
5. 通知对手

---

## 第四阶段：输入系统与配置 (建议 1 天)

### 4.1 Chess.inputactions
**文件**: `Chess/Assets/Chess.inputactions`

**学习要点**:
- Unity 新输入系统 (Input System) 的配置文件
- Action Maps: Player (游戏) 和 UI (界面)
- Actions: Move (移动), Look (查看), Interact (交互)
- Bindings: 键盘、鼠标、手柄、触摸屏的映射

**关键配置**:
- `Interact` 动作绑定到鼠标左键 `<Mouse>/leftButton`
- `Move` 动作使用 WASD 和方向键

**实践任务**:
- [ ] 在 Unity 中打开 Input Actions 编辑器查看
- [ ] 尝试添加新的输入绑定

---

## 第五阶段：第三方库学习 (建议 2-3 天)

### 5.1 Gera.Chess 库
由于这是外部 NuGet 包，需要查阅文档或反编译了解。

**已知 API**:
```csharp
// 创建新棋盘
var chessBoard = new ChessBoard();

// 从 FEN 加载
var chessBoard = ChessBoard.LoadFromFen(fenString);

// 导出为 FEN
string fen = chessBoard.ToFen();

// 验证移动
bool isValid = chessBoard.IsValidMove(new Move("e2", "e4"));

// 执行移动
chessBoard.Move(new Move("e2", "e4"));

// 检查游戏结束
if (chessBoard.IsEndGame) { ... }

// 获取结束信息
var endGame = chessBoard.EndGame;
// endGame.WonSide - 获胜方
// endGame.EndgameType - 结束类型 (将杀、逼和等)

// 当前回合方
PieceColor turn = chessBoard.Turn; // White 或 Black
```

**实践任务**:
- [ ] 编写简单的控制台程序测试 Gera.Chess 功能
- [ ] 了解国际象棋规则在代码中的表示

---

## 第六阶段：Unity Gaming Services (建议 3-5 天)

### 6.1 核心服务概览

| 服务 | 用途 | 代码中的使用 |
|------|------|-------------|
| Authentication | 玩家身份验证 | `SignInAnonymouslyAsync()` |
| Lobby | 游戏房间管理 | 创建、加入、获取房间 |
| Cloud Save | 数据云存储 | 保存/读取游戏状态 |
| Cloud Code | 服务端逻辑 | 所有服务端方法 |
| Leaderboards | 排行榜 | ELO 分数存储 |
| Push Notifications | 实时消息推送 | 通知对手移动 |

### 6.2 学习资源
- [Unity Gaming Services 官方文档](https://docs.unity.com/ugs/)
- 重点学习:
  - Cloud Code 的编写和部署
  - Lobby 的创建和管理
  - Cloud Save 的数据操作
  - 实时消息订阅机制

---

## 第七阶段：进阶与扩展 (建议 5-7 天)

### 7.1 代码改进建议
1. **添加移动历史记录**: 实现棋谱回放功能
2. **添加计时器**: 实现棋钟功能
3. **添加聊天系统**: 玩家间通信
4. **添加观战模式**: 其他玩家可以观看对局
5. **添加 AI 对手**: 使用 Chess AI 库

### 7.2 性能优化
1. 对象池化 (Object Pooling) 减少 Instantiate/Destroy
2. 使用 Sprite 代替 3D 模型提升性能
3. 网络消息压缩

### 7.3 功能扩展
1. 添加用户注册和登录 (替换匿名登录)
2. 添加好友系统
3. 添加比赛模式
4. 添加棋局分析功能

---

## 学习检查清单

### 基础理解
- [ ] 能解释 FEN 记谱法
- [ ] 能描述客户端与服务端的交互流程
- [ ] 能理解 ELO 评分计算
- [ ] 能说明 Unity 新输入系统的基本用法

### 代码阅读
- [ ] 能独立阅读 Player.cs 并理解每个方法
- [ ] 能独立阅读 Chess.cs 并理解服务端逻辑
- [ ] 能追踪从点击棋子到移动完成的完整流程

### 实践操作
- [ ] 能在 Unity 中运行项目并测试功能
- [ ] 能修改代码添加简单功能 (如修改颜色)
- [ ] 能调试网络通信问题

### 扩展能力
- [ ] 能添加一个新的 Cloud Code 函数
- [ ] 能修改 UI 并绑定新功能
- [ ] 能实现一个简单的游戏功能 (如和棋请求)

---

## 推荐学习顺序

```
第 1 天: 第一阶段 (项目结构) + 第二阶段 A (Piece.cs)
第 2-3 天: 第二阶段 B-F (Player.cs 详细学习)
第 4-5 天: 第三阶段 (ChessCloudCode 服务端)
第 6 天: 第四阶段 (输入系统) + 第五阶段 (Gera.Chess)
第 7-9 天: 第六阶段 (UGS 服务深入学习)
第 10-14 天: 第七阶段 (进阶扩展实践)
```

---

## 常见问题 FAQ

**Q: 为什么 Piece.cs 这么简单？**
A: 因为棋子只是视觉表现，所有逻辑 (移动规则、游戏状态) 都在服务端用 Gera.Chess 库处理。

**Q: 如何调试 Cloud Code？**
A: 使用 Unity Dashboard 的 Cloud Code 日志功能，或在代码中使用 `_logger.LogInformation()`。

**Q: 可以离线玩吗？**
A: 当前实现需要联网，因为核心逻辑在云端。可以修改将逻辑移到本地实现单机版。

**Q: 如何添加音效？**
A: 在 `SyncBoard()` 和 `MakeMove()` 中添加 AudioSource.Play() 调用。

---

## 参考资源

1. **国际象棋规则**
   - FEN 记谱法: https://en.wikipedia.org/wiki/Forsyth%E2%80%93Edwards_Notation
   - ELO 评分系统: https://en.wikipedia.org/wiki/Elo_rating_system

2. **Unity 技术**
   - Input System: https://docs.unity3d.com/Packages/com.unity.inputsystem@1.7/manual/index.html
   - Unity Gaming Services: https://docs.unity.com/ugs/

3. **NuGet 包**
   - Gera.Chess: 需要查阅包文档或反编译了解详细 API

---

*祝学习愉快！建议边阅读代码边在 Unity 中运行测试，这样理解会更深刻。*
