# Unity 多人在线国际象棋项目流程文档

## 一、项目概述

本项目是一个基于 Unity Gaming Services 的多人在线国际象棋游戏，采用服务器权威架构，无需专用游戏服务器即可实现异步多人对战。项目使用 FEN（Forsyth-Edwards Notation）格式表示棋盘状态，支持完整的国际象棋规则验证，并集成了 Elo 评分系统。

### 技术栈

- **游戏引擎**: Unity
- **编程语言**: C#
- **后端服务**: Unity Gaming Services (UGS)
  - Cloud Code: 服务器端游戏逻辑
  - Lobby: 游戏大厅系统
  - Cloud Save: 游戏状态持久化
  - Leaderboards: Elo 评分排行榜
  - Authentication: 玩家身份验证
- **第三方库**: 
  - Gera Chess Library: 国际象棋规则验证
  - Newtonsoft.Json: JSON 序列化
  - TextMesh Pro: UI 文本渲染

### 项目结构

```
d:\unity\my_chess/
├── Chess/                          # Unity 客户端项目
│   ├── Assets/
│   │   ├── Resources/              # 棋子模型资源
│   │   │   ├── Bishop/             # 主教资源
│   │   │   ├── Board/              # 棋盘资源
│   │   │   ├── King/               # 国王资源
│   │   │   ├── Knight/             # 骑士资源
│   │   │   ├── Pawn/               # 兵资源
│   │   │   ├── Queen/              # 皇后资源
│   │   │   └── Rook/               # 城堡资源
│   │   ├── Scenes/
│   │   │   └── ChessDemo.unity     # 主场景
│   │   ├── Setup/
│   │   │   ├── ChessCloudCode.ccmr # Cloud Code 模块配置
│   │   │   └── EloRatings.lb       # Elo 排行榜配置
│   │   ├── Piece.cs                # 棋子基类
│   │   └── Player.cs               # 主游戏控制器
│   └── Packages/
├── ChessCloudCode/                 # Cloud Code 服务端项目
│   ├── Chess.cs                    # 游戏逻辑模块
│   └── ModuleConfig.cs             # 依赖注入配置
└── README.md
```

---

## 二、核心模块说明

### 2.1 客户端模块

#### 2.1.1 Piece.cs - 棋子基类

**功能**: 棋子游戏对象的基础组件

**主要职责**:
- 记录棋子的初始位置

**关键属性**:
- `InitialPos`: Vector3 类型，存储棋子的初始世界坐标

**使用场景**:
- 挂载到所有棋子预制体上
- 为棋子重置功能提供位置参考

#### 2.1.2 Player.cs - 主游戏控制器

**功能**: 客户端核心逻辑控制器，处理所有游戏交互

**主要职责**:
1. Unity Services 初始化与认证
2. 游戏大厅创建与加入
3. 棋盘状态同步与渲染
4. 玩家交互处理（选择棋子、移动棋子）
5. 实时消息订阅与处理
6. Elo 评分显示与更新

**关键属性**:

| 属性名 | 类型 | 说明 |
|--------|------|------|
| `_selectedPiece` | GameObject | 当前选中的棋子 |
| `playerCamera` | Camera | 主摄像机引用 |
| `_isWhite` | bool | 当前玩家是否执白棋 |
| `_currentSession` | string | 当前游戏会话 ID（Lobby ID） |
| `_gameStarted` | bool | 游戏是否已开始 |
| `_prefabs` | Dictionary | 棋子预制体缓存 |

**核心方法**:

| 方法名 | 功能说明 |
|--------|----------|
| `Start()` | 初始化 Unity Services、认证、订阅消息、同步初始棋盘 |
| `CreateGame()` | 创建游戏大厅，返回大厅代码 |
| `JoinLobbyByCode()` | 通过大厅代码加入游戏 |
| `MakeMove()` | 发送移动请求到服务器 |
| `SyncBoard()` | 根据 FEN 字符串同步棋盘状态 |
| `PlayerInteract()` | 处理玩家点击交互 |
| `SelectPiece()` | 选中/取消选中棋子 |
| `Resign()` | 认输功能 |

**数据模型**:

```csharp
public class HostGameResponse
{
    public string LobbyCode { get; set; }
}

public class JoinGameResponse
{        
    public string Session { get; set; }
    public string Board { get; set; }
    public string OpponentId { get; set; }
    public bool IsWhite { get; set; }
}

public class BoardUpdateResponse
{
    public string Board { get; set; }
    public bool GameOver { get; set; }
    public string EndgameType { get; set; }
}
```

### 2.2 服务端模块

#### 2.2.1 Chess.cs - Cloud Code 游戏逻辑模块

**功能**: 服务器端游戏逻辑处理，确保游戏规则的权威性

**主要职责**:
1. 游戏大厅管理（创建、加入）
2. 游戏状态存储与加载
3. 移动合法性验证
4. 游戏结束判定
5. Elo 评分计算与更新
6. 实时消息推送

**关键常量**:

| 常量名 | 值 | 说明 |
|--------|-----|------|
| `LeaderboardId` | "EloRatings" | Elo 排行榜 ID |
| `KValue` | 30 | Elo 计算的 K 值 |
| `StartingElo` | 1500 | 初始 Elo 分数 |

**核心端点（Cloud Code Functions）**:

| 端点名 | 功能 | 参数 | 返回值 |
|--------|------|------|--------|
| `HostGame` | 创建游戏大厅 | 无 | HostGameResponse |
| `JoinGame` | 加入游戏大厅 | lobbyCode | JoinGameResponse |
| `MakeMove` | 执行移动 | session, fromPosition, toPosition | BoardUpdateResponse |
| `Resign` | 认输 | session | BoardUpdateResponse |

**关键方法**:

| 方法名 | 功能说明 |
|--------|----------|
| `HostGame()` | 创建 Lobby，初始化棋盘，设置白方玩家 ID |
| `JoinGame()` | 加入 Lobby，随机分配黑白方，通知对手 |
| `StartGame()` | 初始化游戏状态，发送开始消息给双方 |
| `Rejoin()` | 重新加入已存在的游戏 |
| `MakeMove()` | 验证移动合法性，更新棋盘，检查游戏结束，推送更新 |
| `Resign()` | 处理认输逻辑，更新 Elo 评分 |
| `UpdateElos()` | 计算并更新双方 Elo 评分 |

**Elo 评分计算公式**:

```
期望得分 = 1 / (1 + 10^((对手Elo - 玩家Elo) / 400))
Elo变化 = K值 * (实际得分 - 期望得分)
新Elo = 旧Elo + Elo变化
```

#### 2.2.2 ModuleConfig.cs - 依赖注入配置

**功能**: 配置 Cloud Code 模块的依赖注入

**注册的服务**:
- `IGameApiClient`: Unity Gaming Services API 客户端
- `IPushClient`: 消息推送客户端
- `Random`: 随机数生成器（用于随机分配黑白方）

---

## 三、游戏流程详解

### 3.1 游戏初始化流程

```
玩家启动游戏
    ↓
Player.Start() 执行
    ↓
初始化 Unity Services (UnityServices.InitializeAsync)
    ↓
匿名登录 (AuthenticationService.Instance.SignInAnonymouslyAsync)
    ↓
订阅玩家消息 (SubscribeToPlayerMessagesAsync)
    ↓
同步初始棋盘 (SyncBoard - 标准开局 FEN)
    ↓
初始化玩家信息 (InitializePlayer)
    ├─ 尝试获取 Elo 分数
    └─ 若不存在则创建初始分数 1500
    ↓
游戏界面就绪
```

**关键代码位置**: [Player.cs:52-60](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L52-L60)

### 3.2 创建游戏流程

```
玩家点击"创建游戏"按钮
    ↓
Player.CreateGame() 执行
    ↓
调用 Cloud Code: HostGame 端点
    ↓
Chess.HostGame() 服务端执行
    ├─ 创建 Lobby (最大2人，私有)
    ├─ 初始化棋盘状态 (ChessBoard)
    ├─ 保存到 Cloud Save
    │   ├─ board: 初始 FEN 字符串
    │   └─ whitePlayerId: 创建者 ID
    └─ 返回 LobbyCode
    ↓
客户端显示大厅代码
    ↓
等待对手加入...
```

**关键代码位置**: 
- 客户端: [Player.cs:102-115](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L102-L115)
- 服务端: [Chess.cs:31-48](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L31-L48)

### 3.3 加入游戏流程

```
玩家输入大厅代码并点击"加入游戏"
    ↓
Player.JoinLobbyByCode() 执行
    ↓
清理大厅代码（去除空格和特殊字符）
    ↓
调用 Cloud Code: JoinGame 端点
    ↓
Chess.JoinGame() 服务端执行
    ├─ 尝试通过代码加入 Lobby
    ├─ 若失败则检查是否已在 Lobby 中
    ├─ 调用 StartGame()
    │   ├─ 随机分配黑白方
    │   ├─ 更新 Cloud Save
    │   │   ├─ board: FEN 字符串
    │   │   ├─ whitePlayerId: 白方 ID
    │   │   └─ blackPlayerId: 黑方 ID
    │   ├─ 发送 "opponentJoined" 消息给对手
    │   └─ 返回游戏信息
    └─ 返回 JoinGameResponse
    ↓
客户端 OnGameStart() 处理
    ├─ 保存会话 ID
    ├─ 设置对手信息
    ├─ 同步棋盘
    ├─ 设置视角（白方朝上/黑方朝下）
    ├─ 隐藏 UI 面板
    └─ 显示认输按钮
    ↓
游戏开始！
```

**关键代码位置**: 
- 客户端: [Player.cs:137-163](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L137-L163)
- 服务端: [Chess.cs:50-116](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L50-L116)

### 3.4 移动棋子流程

```
玩家点击棋子
    ↓
Player.PlayerInteract() 触发
    ↓
射线检测点击对象
    ├─ 若点击己方棋子 → SelectPiece()
    │   └─ 高亮显示选中棋子
    └─ 若点击棋盘或敌方棋子
        └─ 若已选中棋子 → MakeMove()
    ↓
调用 Cloud Code: MakeMove 端点
    ├─ 参数: session, fromPosition, toPosition
    └─ 位置格式: 代数记谱法 (如 "e2", "e4")
    ↓
Chess.MakeMove() 服务端执行
    ├─ 从 Cloud Save 加载游戏状态
    │   ├─ board (FEN)
    │   ├─ whitePlayerId
    │   └─ blackPlayerId
    ├─ 验证玩家回合
    │   └─ 若不是该玩家回合 → 抛出异常
    ├─ 验证移动合法性（使用 Chess 库）
    │   └─ 若非法移动 → 抛出异常
    ├─ 执行移动
    ├─ 更新 Cloud Save
    ├─ 检查游戏是否结束
    │   ├─ 若结束 → 更新双方 Elo 评分
    │   └─ 计算新 Elo 分数
    ├─ 发送 "boardUpdated" 消息给对手
    └─ 返回 BoardUpdateResponse
    ↓
客户端 OnBoardUpdate() 处理
    ├─ 同步棋盘状态 (SyncBoard)
    │   ├─ 解析 FEN 字符串
    │   ├─ 清除现有棋子
    │   └─ 实例化新棋子
    └─ 若游戏结束
        ├─ 显示结果
        ├─ 隐藏认输按钮
        └─ 刷新玩家 Elo 分数
```

**关键代码位置**: 
- 客户端交互: [Player.cs:283-307](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L283-L307)
- 客户端移动: [Player.cs:203-218](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L203-L218)
- 服务端: [Chess.cs:152-214](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L152-L214)

### 3.5 实时消息订阅流程

```
游戏初始化时订阅消息
    ↓
SubscribeToPlayerMessagesAsync()
    ↓
注册消息回调
    ├─ MessageReceived
    │   ├─ "boardUpdated" → 棋盘更新
    │   │   └─ 反序列化 BoardUpdateResponse
    │   │       └─ 调用 OnBoardUpdate()
    │   └─ "opponentJoined" → 对手加入
    │       └─ 反序列化 JoinGameResponse
    │           └─ 调用 OnGameStart()
    ├─ ConnectionStateChanged
    │   └─ 记录连接状态变化
    ├─ Kicked
    │   └─ 记录被踢出事件
    └─ Error
        └─ 记录错误信息
    ↓
保持长连接，实时接收服务器推送
```

**关键代码位置**: [Player.cs:245-281](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L245-L281)

### 3.6 认输流程

```
玩家点击"认输"按钮
    ↓
Player.Resign() 执行
    ↓
调用 Cloud Code: Resign 端点
    ├─ 参数: session
    ↓
Chess.Resign() 服务端执行
    ├─ 从 Cloud Save 加载游戏状态
    ├─ 确定玩家颜色
    ├─ 执行认输逻辑
    ├─ 更新 Cloud Save
    ├─ 计算并更新双方 Elo 评分
    │   ├─ 认输方: 0 分
    │   └─ 对手: 1 分
    ├─ 发送 "boardUpdated" 消息给对手
    └─ 返回 BoardUpdateResponse
    ↓
客户端 OnBoardUpdate() 处理
    └─ 显示游戏结束信息
```

**关键代码位置**: 
- 客户端: [Player.cs:123-135](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L123-L135)
- 服务端: [Chess.cs:241-283](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L241-L283)

---

## 四、数据流与状态管理

### 4.1 棋盘状态表示

**FEN 字符串格式**:
```
rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1
```

**各部分含义**:
1. 棋盘布局（8行，用 `/` 分隔）
   - 小写字母: 黑方棋子
   - 大写字母: 白方棋子
   - 数字: 连续空格数
2. 当前回合 (`w`=白方, `b`=黑方)
3. 王车易位权限
4. 过路兵目标格
5. 半回合计数
6. 全回合计数

**棋子字母对照表**:

| 字母 | 英文名 | 中文名 |
|------|--------|--------|
| p/P | Pawn | 兵 |
| n/N | Knight | 骑士 |
| b/B | Bishop | 主教 |
| r/R | Rook | 城堡 |
| q/Q | Queen | 皇后 |
| k/K | King | 国王 |

### 4.2 Cloud Save 数据结构

**存储键值对**:

| 键名 | 类型 | 说明 |
|------|------|------|
| `board` | string | 当前棋盘 FEN 字符串 |
| `whitePlayerId` | string | 白方玩家 ID |
| `blackPlayerId` | string | 黑方玩家 ID |

**存储位置**: Lobby ID 作为 Cloud Save 的自定义 ID

### 4.3 客户端状态管理

**Player 类状态变量**:

```csharp
private GameObject _selectedPiece;      // 选中的棋子
private bool _gameStarted;              // 游戏是否开始
private bool _isWhite;                  // 是否执白棋
private string _currentSession;         // 当前会话 ID
private Dictionary<string, UnityEngine.Object> _prefabs;  // 预制体缓存
```

---

## 五、关键技术实现

### 5.1 FEN 字符串解析

**方法**: `FenToDict(string fen)`

**功能**: 将 FEN 字符串转换为坐标-棋子字典

**实现逻辑**:
1. 分割 FEN 字符串获取棋盘部分
2. 按行分割（`/` 分隔符）
3. 遍历每行字符
   - 数字: 跳过对应数量的格子
   - 字母: 记录棋子类型和坐标
4. 返回 `Dictionary<Tuple<int, int>, char>`

**坐标系统**:
- X 轴: 0-7（对应 a-h 列）
- Y 轴: 0-7（对应 1-8 行，从下到上）

**关键代码位置**: [Player.cs:321-351](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L321-L351)

### 5.2 棋盘同步与渲染

**方法**: `SyncBoard(string fen)`

**功能**: 根据 FEN 字符串实例化棋子

**实现流程**:
1. 解析 FEN 字符串为字典
2. 销毁现有棋子对象
3. 遍历字典，为每个棋子:
   - 确定棋子类型（Pawn/Knight/Bishop 等）
   - 确定颜色（Light/Dark）
   - 加载对应预制体
   - 实例化棋子对象
   - 设置位置和旋转
     - 白方棋子: 旋转 0°
     - 黑方棋子: 旋转 180°

**关键代码位置**: [Player.cs:165-201](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L165-L201)

### 5.3 位置坐标转换

**方法**: `PosToFen(Vector3 pos)`

**功能**: 将 Unity 世界坐标转换为代数记谱法

**转换逻辑**:
```csharp
x 坐标 (0-7) → 字母 (a-h): (char)(pos.x + 97)
z 坐标 (0-7) → 数字 (1-8): (char)pos.z + 1
```

**示例**:
- Vector3(0, 0, 0) → "a1"
- Vector3(4, 0, 3) → "e4"
- Vector3(7, 0, 7) → "h8"

**关键代码位置**: [Player.cs:379-382](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L379-L382)

### 5.4 视角系统

**方法**: `SetPov()`

**功能**: 根据玩家颜色设置摄像机视角

**实现**:
- 白方玩家: 摄像机角度 0°（正常视角）
- 黑方玩家: 摄像机角度 180°（翻转视角）

**关键代码位置**: [Player.cs:117-121](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L117-L121)

### 5.5 射线检测交互

**方法**: `PlayerInteract(InputAction.CallbackContext context)`

**功能**: 处理玩家鼠标点击交互

**实现流程**:
1. 获取鼠标屏幕坐标
2. 从摄像机发射射线
3. 检测射线碰撞对象
   - 点击棋盘: 尝试移动到该位置
   - 点击己方棋子: 选中棋子
   - 点击敌方棋子: 尝试吃子
4. 更新选中状态和高亮显示

**关键代码位置**: [Player.cs:283-307](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L283-L307)

---

## 六、Unity Gaming Services 集成

### 6.1 Authentication（身份认证）

**使用方式**: 匿名认证

**初始化流程**:
```csharp
await UnityServices.InitializeAsync();
await AuthenticationService.Instance.SignInAnonymouslyAsync();
```

**获取玩家信息**:
- `AuthenticationService.Instance.PlayerId`: 玩家唯一 ID

**关键代码位置**: [Player.cs:52-56](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L52-L56)

### 6.2 Lobby（游戏大厅）

**功能**: 管理游戏房间和玩家匹配

**关键操作**:
- 创建大厅: `CreateLobbyAsync()`
- 加入大厅: `JoinLobbyByCodeAsync()`
- 获取大厅列表: `GetJoinedLobbiesAsync()`

**大厅配置**:
- 名称: `{PlayerId}'s game`
- 最大玩家数: 2
- 私有: true
- 公开: false

**关键代码位置**: [Chess.cs:34-35](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L34-L35)

### 6.3 Cloud Save（云存储）

**功能**: 持久化游戏状态

**存储模式**: 自定义 ID 存储（使用 Lobby ID）

**关键操作**:
- 保存数据: `SetCustomItemBatchAsync()` / `SetCustomItemAsync()`
- 加载数据: `GetCustomItemsAsync()`

**数据项**:
```csharp
new SetItemBody("board", chessBoard.ToFen())
new SetItemBody("whitePlayerId", playerId)
new SetItemBody("blackPlayerId", opponentId)
```

**关键代码位置**: [Chess.cs:37-42](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L37-L42)

### 6.4 Leaderboards（排行榜）

**功能**: 管理玩家 Elo 评分

**排行榜 ID**: `EloRatings`

**关键操作**:
- 添加分数: `AddPlayerScoreAsync()`
- 获取玩家分数: `GetPlayerScoreAsync()`
- 批量查询: `GetScoresByPlayerIdsAsync()`

**初始分数**: 1500

**关键代码位置**: 
- 初始化: [Player.cs:62-82](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L62-L82)
- 更新: [Chess.cs:216-239](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L216-L239)

### 6.5 Cloud Code（云代码）

**功能**: 服务器端游戏逻辑执行

**模块名**: `ChessCloudCode`

**端点列表**:

| 端点名 | 功能 | 调用时机 |
|--------|------|----------|
| `HostGame` | 创建游戏 | 玩家点击"创建游戏" |
| `JoinGame` | 加入游戏 | 玩家输入代码加入 |
| `MakeMove` | 移动棋子 | 玩家移动棋子 |
| `Resign` | 认输 | 玩家点击"认输" |

**调用方式**:
```csharp
await CloudCodeService.Instance.CallModuleEndpointAsync<TResponse>(
    "ChessCloudCode", 
    "EndpointName",
    new Dictionary<string, object> { { "param", value } }
);
```

**关键代码位置**: [Player.cs:106](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L106)

### 6.6 Push Notifications（消息推送）

**功能**: 实时向玩家推送游戏更新

**消息类型**:
- `opponentJoined`: 对手加入游戏
- `boardUpdated`: 棋盘状态更新

**订阅方式**:
```csharp
var callbacks = new SubscriptionEventCallbacks();
callbacks.MessageReceived += @event => { /* 处理消息 */ };
await CloudCodeService.Instance.SubscribeToPlayerMessagesAsync(callbacks);
```

**推送方式**:
```csharp
await _pushClient.SendPlayerMessageAsync(
    context,
    JsonConvert.SerializeObject(message),
    "messageType",
    playerId
);
```

**关键代码位置**: 
- 订阅: [Player.cs:245-281](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L245-L281)
- 推送: [Chess.cs:103-107](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L103-L107)

---

## 七、部署与运行

### 7.1 环境要求

**Unity 版本**: 2021.3 或更高

**.NET 要求**: 
- 需要安装 .NET SDK
- 配置路径: `Edit > Preferences… > Cloud Code > .NET path`

**Unity 服务配置**:
1. 在 Unity Dashboard 创建项目
2. 获取项目 ID
3. 在 Unity Editor 中配置项目 ID

### 7.2 部署步骤

#### 方式一：使用 Deployment Window

1. 打开 Unity 项目 `Chess` 文件夹
2. 打开场景 `Assets/Scenes/ChessDemo.unity`
3. 打开 Deployment 窗口
   - Unity 2021 及之前: `Window > Deployment`
   - Unity 2022 及之后: `Services > Deployment`
4. 点击 `Deploy All` 部署:
   - `ChessCloudCode.ccmr` (Cloud Code 模块)
   - `EloRatings.lb` (排行榜)

#### 方式二：使用 UGS CLI

1. 安装并配置 UGS CLI
2. 运行部署命令:
```bash
ugs deploy ChessCloudCode/ChessCloudCode.sln --services cloud-code-modules
ugs deploy Chess/Assets/Setup/EloRatings.lb --services leaderboards
```

或直接运行部署脚本:
```bash
./deploy.sh
```

### 7.3 运行测试

**单机测试**:
1. 在 Unity Editor 中运行场景
2. 点击"创建游戏"创建房间
3. 记录显示的大厅代码

**多人测试**:
1. 构建游戏: `File > Build and Run`
2. 在构建版本中创建游戏
3. 在 Unity Editor 中使用大厅代码加入
4. 开始对战

**测试流程**:
```
客户端 A (创建者)           客户端 B (加入者)
    |                            |
    ├─ 创建游戏                   |
    ├─ 获得代码: "ABCD12"         |
    |                            |
    |                            ├─ 输入代码加入
    |                            ├─ 随机分配颜色
    ├─ 收到对手加入通知 ←─────────┤
    ├─ 游戏开始                   ├─ 游戏开始
    |                            |
    ├─ 移动棋子                   |
    |                            ├─ 收到棋盘更新
    |                            ├─ 移动棋子
    ├─ 收到棋盘更新 ←─────────────┤
    |                            |
    └─ ... 对战进行中 ...          └─
```

---

## 八、安全性与权限控制

### 8.1 服务器权威架构

**设计原则**:
- 所有游戏逻辑在 Cloud Code 中执行
- 客户端只负责渲染和用户输入
- 服务器验证所有操作合法性

**优势**:
- 防止作弊（客户端无法修改游戏规则）
- 确保游戏状态一致性
- 公平的 Elo 评分系统

### 8.2 移动验证

**验证步骤**:
1. 验证玩家身份（是否在游戏中）
2. 验证回合（是否轮到该玩家）
3. 验证移动合法性（使用 Chess 库）
4. 执行移动并更新状态

**关键代码位置**: [Chess.cs:172-186](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L172-L186)

### 8.3 访问控制

**建议配置**:
使用 Unity Access Control 限制 Cloud Code 端点访问权限，确保只有认证玩家可以调用特定端点。

**参考文档**: [Access Control](https://docs.unity.com/ugs-overview/en/manual/access-control)

---

## 九、性能优化

### 9.1 预制体缓存

**实现**: 使用字典缓存已加载的预制体

```csharp
private readonly Dictionary<string, UnityEngine.Object> _prefabs = new();

if (!_prefabs.ContainsKey(prefabName))
{
    _prefabs[prefabName] = Resources.Load($"{pieceType}/Prefabs/{prefabName}");
}
```

**优势**: 避免重复加载相同资源

**关键代码位置**: [Player.cs:187-190](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L187-L190)

### 9.2 异步操作

**使用场景**:
- Unity Services 初始化
- 网络请求（Cloud Code、Lobby、Cloud Save）
- 消息订阅

**实现方式**: 使用 `async/await` 模式

**关键代码位置**: [Player.cs:52-60](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L52-L60)

---

## 十、错误处理

### 10.1 客户端错误处理

**常见错误**:
- 大厅代码无效
- 网络连接失败
- 认证失败

**处理方式**:
```csharp
try
{
    // 执行操作
}
catch (Exception exception)
{
    Debug.LogException(exception);
    resultText.text = "错误提示信息";
    uiPanel.SetActive(true);
}
```

**关键代码位置**: [Player.cs:109-114](file:///d:/unity/my_chess/Chess/Assets/Player.cs#L109-L114)

### 10.2 服务端错误处理

**常见错误**:
- 玩家不在游戏中
- 不是当前玩家的回合
- 非法移动

**处理方式**:
```csharp
if (chessBoard.Turn != playerColour)
{
    throw new Exception("Invalid move, not active player");
}
```

**关键代码位置**: [Chess.cs:173-177](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L173-L177)

---

## 十一、扩展建议

### 11.1 功能扩展

**建议添加功能**:
1. **观战模式**: 允许其他玩家观看对局
2. **回放系统**: 记录对局并支持回放
3. **好友系统**: 添加好友并邀请对战
4. **聊天功能**: 对局中实时聊天
5. **计时器**: 添加思考时间限制
6. **多种棋盘主题**: 提供不同的棋盘和棋子样式

### 11.2 技术优化

**建议优化方向**:
1. **对象池**: 使用对象池管理棋子实例，减少实例化开销
2. **增量更新**: 只更新移动的棋子，而非重建整个棋盘
3. **离线模式**: 添加本地双人对战模式
4. **AI 对战**: 集成国际象棋 AI 引擎
5. **断线重连**: 实现更完善的断线重连机制

### 11.3 UI/UX 改进

**建议改进**:
1. **移动提示**: 显示合法移动位置
2. **动画效果**: 棋子移动动画
3. **音效**: 棋子移动、吃子音效
4. **教程系统**: 新手引导
5. **历史记录**: 显示移动历史

---

## 十二、常见问题

### Q1: 如何调试 Cloud Code 模块？

**方法**:
1. 使用 `ILogger` 记录日志
2. 在 Unity Dashboard 查看 Cloud Code 日志
3. 使用 `Debug.Log()` 在客户端记录

**关键代码位置**: [Chess.cs:175](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L175)

### Q2: 如何处理网络延迟？

**建议**:
1. 在客户端显示加载动画
2. 实现移动预测（乐观 UI）
3. 添加超时处理机制

### Q3: 如何防止重复加入游戏？

**解决方案**:
服务端检查玩家是否已在 Lobby 中，若已在则调用 `Rejoin()` 方法重新加入。

**关键代码位置**: [Chess.cs:58-78](file:///d:/unity/my_chess/ChessCloudCode/Chess.cs#L58-L78)

### Q4: 如何处理玩家中途退出？

**当前实现**: 
游戏继续，对手可以继续移动

**建议改进**:
添加超时机制，若玩家长时间未操作则自动判负

---

## 十三、参考资料

### 13.1 Unity 官方文档

- [Unity Gaming Services](https://unity.com/solutions/gaming-services)
- [Cloud Code 文档](https://docs.unity.com/cloud-code)
- [Lobby 文档](https://docs.unity.com/lobby)
- [Cloud Save 文档](https://docs.unity.com/cloud-save)
- [Leaderboards 文档](https://docs.unity.com/leaderboards)
- [Authentication 文档](https://docs.unity.com/authentication)

### 13.2 第三方资源

- [Gera Chess Library](https://github.com/Geras1mleo/Chess): 国际象棋规则验证库
- [Free Low Poly Chess Set](https://assetstore.unity.com/packages/3d/props/free-low-poly-chess-set-116856): 棋子模型资源

### 13.3 国际象棋规则

- [FIDE 国际象棋规则](https://www.fide.com/FIDE/handbook/LawsOfChess.pdf)
- [FEN 格式说明](https://en.wikipedia.org/wiki/Forsyth%E2%80%93Edwards_Notation)
- [Elo 评分系统](https://en.wikipedia.org/wiki/Elo_rating_system)

---

## 十四、版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | - | 初始版本，实现基础多人对战功能 |

---

## 十五、许可证

本项目采用 Unity 技术开发，使用 Unity Gaming Services。详见 [LICENSE.md](file:///d:/unity/my_chess/LICENSE.md)。

---

**文档生成日期**: 2026-05-04  
**项目路径**: d:\unity\my_chess  
**Unity 版本**: 2021.3+  
**文档版本**: 1.0
